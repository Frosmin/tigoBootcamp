@startuml notificaciones-03-bp-consulta-historial
title BP - Estado e historial paginado
start
if (¿GET /notifications/{id}?) then (sí)
  :Validar BIGINT;
  :Consultar notificación e intentos ordenados;
  :200 o 404;
else (listado)
  :Validar canal, estado, limit y cursor;
  :Decodificar cursor created_at + id;
  :Consulta keyset con limit + 1;
  :Devolver items y nextCursor;
endif
stop
@enduml
